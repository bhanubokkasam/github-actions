name: Goose Terraform Plan Analysis

on:
  pull_request:
    types: [opened, synchronize, reopened, labeled]
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write
  issues: write
 
env:
   PROVIDER_API_KEY: ${{ secrets.API_KEY }}
   GOOGLE_API_KEY: ${{ secrets.API_KEY }}
   PR_NUMBER: ${{ github.event.pull_request.number }}
   GH_TOKEN: ${{ github.token }}  

jobs:
  terraform-plan:
    name: "Terraform Plan"
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1          

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v1

      - name: Terraform Init
        working-directory: terraform
        run: terraform init

      - name: Terraform Plan
        working-directory: terraform
        run: |
          terraform plan -no-color -out=tfplan
          terraform show -no-color tfplan > tf_plan.txt

      - name: Debug - List Terraform Directory Contents
        working-directory: terraform
        run: ls -la

      - name: Upload Terraform Plan Artifact
        uses: actions/upload-artifact@v4
        with:
          name: terraform-plan
          path: terraform/tf_plan.txt

  goose-analysis:
    name: "Goose Analysis and Comment"
    runs-on: ubuntu-latest
    needs: terraform-plan
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download Terraform Plan Artifact
        uses: actions/download-artifact@v4
        with:
          name: terraform-plan
          path: .

      - name: Debug - List Downloaded Files
        run: ls -la

      - name: Gather PR information
        run: |
          {
          echo "# Terraform Plan Preview"
          cat tf_plan.txt
          } > changes.txt

      - name: Install Goose CLI
        run: |
          mkdir -p /home/runner/.local/bin
          curl -fsSL https://github.com/block/goose/releases/download/stable/download_cli.sh \
          | CONFIGURE=false INSTALL_PATH=/home/runner/.local/bin bash
          echo "/home/runner/.local/bin" >> $GITHUB_PATH

      - name: Configure Goose
        run: |
          mkdir -p ~/.config/goose
          cat <<EOF > ~/.config/goose/config.yaml
          GOOSE_PROVIDER: google
          GOOSE_MODEL: gemini-2.0-flash
          keyring: false
          EOF

      - name: Create instructions for Goose
        run: |
          cat <<EOF > instructions.txt
          Analyze the following Terraform plan and provide a human-readable summary that explains:
          - Which resources will be added, changed, or destroyed with their names.
          - The high-level impact of these changes.
          Do not include debug or session details or questions at the end of your output.
          
          Terraform Plan:
          $(cat tf_plan.txt)
          EOF

      - name: Run Goose and filter output
        run: |
          goose run --instructions instructions.txt | \
          sed -E 's/\x1B\[[0-9;]*[mK]//g' | \
          grep -v "logging to /home/runner/.config/goose/sessions/" | \
          grep -v "^starting session" | \
          grep -v "^Closing session" | \
          sed 's/[[:space:]]*$//' \
          > pr_comment.txt

      - name: Post comment to PR
        run: |
          gh pr comment ${{ github.event.pull_request.number }} --body-file pr_comment.txt